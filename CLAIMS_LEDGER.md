# Claim ledger

| Public claim | Evidence |
|---|---|
| The app runs without third-party Python dependencies. | `requirements.txt`; standard-library imports in `app/`. |
| The app supports a local CSV/XLSX audit workflow. | `tests/test_audit_engine.py`, `tests/test_server.py`. |
| Numeric non-finite values and -100% markup conversion are controlled. | `tests/test_external_regressions.py`. |
| The app supports optional hierarchy-aware mapping and a fail-closed HeavyBid-style resource-export profile. | `app/heavybid_adapter.py`, `tests/test_heavybid_adapter.py`, `tests/test_server_profile_detection.py`. |
| The app supports temporary local human finding dispositions and review CSV export. | `app/finding_review.py`, `tests/test_finding_review.py`, `tests/test_server_review.py`. |
| Temporary review sessions use per-token in-memory synchronization for state-sensitive operations and a 30-minute idle timeout refreshed by valid access; concurrent replay of one bulk plan allows at most one apply while different tokens remain independently concurrent. | `app/session_state.py`, `app/server_legacy.py`, `app/server.py`, `app/review_view_context.py`, `app/bulk_review_ui.py`, `tests/test_session_state.py`, `tests/test_session_idle_http.py`, `tests/test_bulk_review_ui.py`. |
| Public runtime feature installation is owned by one explicit deterministic idempotent startup composer; onboarding import/reload is side-effect free and review-only renderer augmentation uses stable internal page identity rather than user-visible title text. | `app/runtime_composition.py`, `app/runtime_context.py`, `app/server.py`, `app/onboarding.py`, `app/bulk_review_ui.py`, `app/review_view_context.py`, `app/operational_reference_ui.py`, `app/review_delta_verification.py`, `tests/test_runtime_composition.py`. |
| Bulk human finding dispositions use a fail-closed plan v2 with explicit targets, supported status/reason validation, human ownership, expected current states, full/selected finding fingerprints, and a plan content digest; pure apply-to-copy revalidates those controls before producing a separate mapping. | `app/bulk_review.py`, `tests/test_bulk_review.py`, `product/bulk_review_contract.md`. |
| The browser bulk-review flow targets only individually checked rows, requires ownership plus preview and separate confirmation, never treats filters/hidden rows as implicit scope, revalidates a one-time plan immediately before one atomic session assignment, and rejects stale/replayed plans without partial updates. | `app/bulk_review_ui.py`, `app/bulk_review.py`, `tests/test_bulk_review_ui.py`, `product/bulk_review_contract.md`. |
| Review saves and bulk review preview/cancel/apply/error paths can preserve the current presentation-only findings/reference filters, searches, sorting, and grouping through a sanitized local view-query context; only known view keys survive and the server constructs the `/results` return URL itself. | `app/review_view_context.py`, `app/bulk_review_ui.py`, `tests/test_review_view_context.py`. |
| The app reports presentation-only Open/Needs correction/reference-exception attention counts with explicit non-readiness empty-state language. | `app/review_guidance.py`, `tests/test_review_guidance.py`, `tests/test_server_attention.py`. |
| The app supports presentation-only findings filters, deterministic sorting/grouping, and stable temporary results navigation without mutating deterministic findings or dispositions. | `app/review_filters.py`, `tests/test_review_filters.py`, `tests/test_server_filters.py`. |
| Grouped findings/reference views can add compact composition summaries to group headings while preserving every ordered evidence row and leaving findings, review state, reference results, metadata, scores, mappings, and exports unchanged. | `app/group_summaries.py`, `app/review_filters.py`, `app/reference_views.py`, `tests/test_group_summaries.py`, `tests/test_review_filters.py`, `tests/test_reference_views.py`. |
| The review page includes keyboard-friendly skip links, focus targets, visible focus styling, and a findings table caption. | `app/server.py`, `tests/test_server_filters.py`. |
| The public local UI adds a document language, one global skip-to-main target, a focusable main landmark, assertive error alerts, and polite status semantics without changing review state. | `app/accessibility.py`, `app/onboarding.py`, `tests/test_accessibility_semantics.py`. |
| The app exports a deterministic local ZIP review package without embedding original estimate/reference bytes. | `app/review_package.py`, `tests/test_review_package.py`, `tests/test_server_package.py`. |
| Review-package ZIPs include deterministic member integrity metadata and can be verified in memory for allowed structure, byte sizes, SHA-256 values, and supported package identity without restoring session state. Strict verification inspects every ZIP entry and rejects directory entries rather than ignoring them. | `app/review_package.py`, `tests/test_review_package.py`, `tests/test_review_package_integrity.py`, `tests/test_server_package.py`. |
| After integrity succeeds, the verifier also checks package-v1 semantic consistency across manifest safety state, finding/review evidence, review dispositions/counts, and optional reference evidence; internally re-hashed but semantically inconsistent snapshots fail closed. | `app/package_preview.py`, `app/review_package.py`, `tests/test_package_preview.py`, `tests/test_package_verification_ui.py`. |
| The local browser exposes a bounded read-only verified-package snapshot preview with escaped finding/review/reference evidence and metadata; it does not execute packaged HTML/README content and creates/restores no review session. | `app/package_preview.py`, `app/package_verification.py`, `tests/test_package_preview.py`, `tests/test_package_verification_ui.py`. |
| A separate acknowledged archived-review continuation flow can recreate only verified archived finding/disposition/reference evidence in a temporary `archived_review_snapshot` session, while leaving source estimate/reference/package bytes absent, preventing remap/re-audit/reference-rerun source state, and recording source package SHA-256 plus explicit no-re-audit provenance in re-exported packages. | `app/archived_review.py`, `app/archived_review_ui.py`, `tests/test_archived_review.py`, `tests/test_archived_review_ui.py`, `product/archived_review_contract.md`. |
| Review Delta can compare two independently verified archived review-package snapshots in memory, separate deterministic finding-evidence drift from human review-state drift, compare governed reference checks and role-linked reference metadata, fail closed on ambiguous duplicate anchors, and create no review session or quality/readiness judgement. | `app/review_delta.py`, `app/review_delta_ui.py`, `tests/test_review_delta.py`, `tests/test_review_delta_ui.py`, `product/review_delta_contract.md`. |
| Review Delta can export a separate deterministic evidence ZIP after independently re-verifying both input review packages; the bundle contains full comparison JSON, spreadsheet-safe delta CSVs, lineage/count/safety manifest, README, and SHA-256/size integrity metadata while creating no session, embedding no original package/source bytes, and making no correctness/readiness/HeavyBid-validation inference. | `app/review_delta_export.py`, `app/review_delta_ui.py`, `tests/test_review_delta_export.py`, `product/review_delta_contract.md`. |
| A portable Review Delta evidence ZIP can be independently verified in memory for exact structure, member hashes/sizes, supported comparison safety/change vocabularies, unique anchors, counts recomputed from actual change rows, and exact manifest/CSV regeneration from canonical JSON; the read-only UI renders bounded escaped evidence and creates no session. | `app/review_delta_export.py`, `app/review_delta_verification.py`, `tests/test_review_delta_verification.py`, `tests/test_review_delta_verification_semantics.py`, `product/review_delta_contract.md`. |
| The original 2026-08-19 public-snapshot allowlist/hash record is preserved as dated historical provenance and is explicitly not current-tree integrity evidence; there is no generated current-release source-tree allowlist manifest at present. | `provenance/initial_public_snapshot_2026-08-19/ALLOWLIST_MANIFEST.json`, `provenance/README.md`, `tests/test_public_snapshot_provenance.py`. |
| The app can validate explicitly supplied Activity/Resource codes and units against governed CSV snapshots without guessing replacements or converting physical units. | `app/reference_validation.py`, `tests/test_reference_validation.py`, `tests/test_server_references.py`. |
| Governed reference uploads can record role, filename, user-supplied revision/label, byte size, and SHA-256 as temporary-session evidence without establishing authority. | `app/reference_metadata.py`, `app/reference_session.py`, `tests/test_reference_metadata.py`, `tests/test_server_reference_metadata.py`. |
| A source-backed audit can expose separate row-linked Crew Code / Production Rate evidence only when Activity plus an operational field is explicitly mapped and an overlapping Operational Activity CSV is explicitly supplied; the app records upload SHA/revision evidence, infers/calculates no operational values, clears stale comparisons when mapped source evidence changes, and keeps this evidence outside review-package v1, archived continuation, Review Delta, and the standard reference CSV. | `app/audit_engine.py`, `app/heavybid_adapter.py`, `app/operational_reference.py`, `app/operational_reference_ui.py`, `app/reference_session.py`, `tests/test_operational_reference.py`, `tests/test_operational_reference_ui.py`, `product/operational_reference_ui_contract.md`. |
| Governed reference checks support presentation-only exception/status/type filtering, metadata-aware search, deterministic sorting/grouping, and composed findings/reference view state without filtering exports. | `app/reference_views.py`, `app/reference_session.py`, `tests/test_reference_views.py`, `tests/test_server_reference_views.py`. |
| A local fictional onboarding walkthrough can start the existing synthetic sample flow and teaches review views, dispositions, reference evidence, package export, explicit bulk review, archived review continuation, and Review Delta while repeating safety boundaries. | `app/onboarding.py`, `product/onboarding_walkthrough.md`, `tests/test_onboarding.py`, `tests/test_bulk_review_ui.py`, `tests/test_archived_review_ui.py`, `tests/test_review_delta_ui.py`. |
| The onboarding guide exposes only a fixed bundled structured fictional estimate and fixed synthetic Activity/Resource reference downloads; demo references are never auto-applied. | `app/demo_kit.py`, `app/onboarding.py`, `app/server.py`, `tests/test_demo_kit.py`, `tests/test_demo_kit_server.py`. |
| Bundled estimate and reference examples are fictional/synthetic. | `samples/` fixtures and corresponding tests. |
| The app is not production-ready and does not certify a bid, reference authority, or HeavyBid import. | `product/PRD.md`, `qa/known_limitations.md`, `product/review_ux_milestone.md`, `product/post_consolidation_roadmap.md`, and `README.md`. |

Unknown: production accuracy, savings, compatibility with every estimating export, customer adoption, willingness to pay, commercial validation, and production HeavyBid import safety.